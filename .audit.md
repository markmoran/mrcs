# MRCS v5 Wireframe Audit
Comparing each page HTML against Figma v.4 source of truth.

---

## 1. Service Detail Page (`mrcs-wf-service-v5.html`) — Figma node 156:201

### Hero
- **Content width**: Figma uses ~850px effective (300px left + 290px right padding on 1440). Current uses `max-width: 800px`. → Widen to 850px.
- **Missing inline 2-col block**: Figma has a block mid-hero (before the sub-headings): text left (~440px) + image right (~354×362px). Current has no such block. → Add.

### Locations Section
- **"Locations" heading missing**: Figma shows a bold "Locations" label (20px) + intro paragraph together in an 800px-wide heading block. Current only has the paragraph. → Add "Locations" heading.
- **Map**: Figma map is full-width of section (1240px effective at 100px side padding), 485px tall. Current map is inside `.container` (1200px max), 360px tall. → Go full section width (no container), 485px tall.
- **Location layout completely wrong**:
  - Figma: 4-column grid of simple location cards (Location Name underlined link + address + city/state + phone). 1200px wide grid. No photo thumbnails.
  - Current: filter dropdowns, count text, 5-row list with metadata icons (pin/phone/email/clock), 120×90px photo thumbnails, pagination. NONE of this is in Figma.
  - → Remove filters, count, row list, photos, pagination. Replace with 4-col location card grid.
- **"View All Locations" button**: Figma has this below the grid. Current has none. → Add button.

### Callout Section
- **Media size**: Figma shows 400×300px. Current is 280×220px. → Fix to 400×300.

---

## Pages to audit next:
- [ ] Get Help / Services overview (155:2548)
- [ ] Home (155:1330)
- [ ] About (156:3763)
- [ ] Careers (156:3535)
- [ ] ESS (165:2390)
- [ ] Locations listing (157:4047)
- [ ] Location detail (190:94)
- [ ] News & Events (158:1533)
- [ ] News archive (161:520)
- [ ] Donate (162:3261)
- [ ] Contact (162:2793)
- [ ] Event post (162:2558)
- [ ] News post (161:909)
- [ ] Generic Page template (155:2326)

---

## 2. Get Help / Services Overview (`mrcs-wf-services-v5.html`) — Figma node 155:2548

### Hero
- **Content width**: Figma shows `w-[850px]` with `px-[80px]` section padding. Current uses nested `.container` (1200px) + `.page-header-inner` (800px) — double container, wrong width. → Use single centered 850px block.
- Structure (breadcrumb, H1, body paragraphs) is correct.

### Services Grid
- Cards structure (name + description + "Learn more" link) matches Figma. Layout is correct.
- Grid at `w-[1260px]` in Figma; current container is 1200px — minor, acceptable.

### Behavioral Health Section
- 2-col layout (text ~589px | video ~589px) matches current implementation. Correct.

### Testimonial
- Figma: `pl-[455px] pr-[445px]` on 1440 = ~540px effective content width. Current uses `max-width: 760px`. → Narrow to ~540px.


---

## 3. Home Page (`mrcs-wf-home-v5.html`) — Figma node 155:1330

### Hero
- Figma: `px-[100px]` on 1440 = 1240px inner. Current uses `.container` (max-width: 1200px). Minor difference.
- The hero section in Figma contains ALL of: H1 + paragraph + buttons + 2 quick-path CTAs ("I need help" / "I'm looking for work") + a crisis mini-CTA inline. Current HTML separates these as: hero → quick-paths section → mrcs-crisis shared component. Structure is fragmented vs what Figma shows as one continuous hero block.
- → The quick paths and crisis CTA should probably be part of the hero section visually, not separate sections.

### Services Cards
- Correct — shared component matches.

### News & Events
- Section structure (news left, events right) matches.
- News items: image thumb (120×385px based on widths) + content. Current has `.news-thumb` + `.news-body`. Close.

### Donate / Support section
- Figma: "Support our mission" label + "Make a difference in your community" heading + "Donate Now" button only — single column.
- Current has 2-col grid with support links (Foundation & Donors, Ways to Give, Our Impact). Extra content not in Figma.
- → Simplify to single-column with just the donate CTA.


---

## 4. About Page (`mrcs-wf-about-v5.html`) — Figma node 156:3763

### Hero
- Figma: Hero section (white bg, `px-[100px] py-[80px]`) contains breadcrumb + H1 + subtitle + Mission & Story 2-col subsection (text ~589px left, 400px video right, "View Current Openings" button) + mini-Crisis CTA — all in one white section.
- Current: Hero only has breadcrumb + H1 + subhead + 3 body paragraphs. Mission & Story is a separate gray `#f6f6f6` section. Crisis is a separate dark shared component. → Wrong: Mission & Story should be inside the hero (white bg), not a separate gray section.
- Hero content width: 850px (current is 800px).

### Locations
- Figma: `px-[100px] py-[80px]` section padding. Heading 800px. Map full-width within section (~1240px). 8-card 4-col grid at 1200px. "View All Locations" button.
- Current: Uses `.container` (1200px). Structure is correct but map is limited to 1200px not ~1240px. Map missing `box-shadow`.
- → Minor: widen map and add shadow.

### Team and Board
- Structure (2 alternating rows: Our Team text+video, The Board video+text) matches Figma. Correct.


---

## 5. Careers Page (`mrcs-wf-careers-v5.html`) — Figma node 156:3535

- **Crisis section position wrong**: Figma order is Hero → career CTA → crisis CTA → stats → benefits → internships → outreach services → services → career CTA → footer. Current HTML has crisis injected AFTER stats, not before it.
- **Outreach services section missing**: Figma has a distinct `outreach services` section (gray `#f5f5f5` bg) with two cards (veteran services + another). Current HTML has a `social-feed` section instead. These are structurally completely different — wrong section entirely.
- **Stats section row 2 reversed**: Figma has both rows as text-left + image-right. Current HTML alternates (row 2 is image-left + text-right). → Fix row 2 to match row 1 direction.
- **Benefits grid**: Figma flex row, content `w-[589px]` + media `w-[400px]`. Current uses `3fr 2fr` grid — minor, close enough.

---

## 6. ESS Page (`mrcs-wf-ess-v5.html`) — Figma node 165:2390

- **Hero content width**: 850px (current is 800px) — same pattern as other pages.
- **No missing sections** — section order matches (Hero → crisis → manufacturing overview × 2 → services → career CTA → footer).
- **No structural errors** — the 2-col content+media layout for both manufacturing sections is correct.

---

## 7. Locations Listing (`mrcs-wf-locations-v5.html`) — Figma node 157:4047

- **Hero content width**: 850px (current is 800px).
- **Map height**: Figma shows 514px. Current is 400px. → Fix to 514px.
- **Location card image size**: Figma uses 250px wide images. Current uses 160px wide. → Fix to 250px.
- **Location card missing background**: Figma cards have `bg-[#f6f6f6]` with `border: #e9e9e9` and `border-radius: 10px`. Current rows have no background/border/radius treatment. → Add card bg, border, radius.

---

## 8. Donate Page (`mrcs-wf-donate-v5.html`) — Figma node 162:3261

- **Hero content width**: 850px (current is 800px).
- **Donation form section heading**: Figma says "Donation Form (Placeholder)". Current says "Make a Donation". → Match Figma label.
- **Donation type dropdown label**: Figma says "Inquiry Type" / "Select an inquiry type". Current says "Donation Type" / "Select a donation type". → Match Figma.
- **Our Supporters section**: Figma shows text-centered. Current is left-aligned. → Center text.
- **Supporter placeholder height**: Figma 97px, current 90px. → Fix.

---

## 9. News & Events (`mrcs-wf-news-events-v5.html`) — Figma node 158:1533

- **Procurement Notices container width**: Figma `px-[200px]` on 1440 = ~1040px inner. Current `max-width: 920px`. → Widen to ~1040px.
- **Section order and structure otherwise correct** — 3-col news grid, 3-col events grid, procurement grid all match.

---

## 10. News Post (`mrcs-wf-news-post-v5.html`) — Figma node 161:909

- **Article content width**: 850px (current is 800px).
- **Author bio card**: Figma places it inside the article body section (white bg, no visual section break). Current puts it in a separate `<section id="author-bio">` with its own padding. → Move author bio inside article body, remove separate section wrapper.

---

## 11. Contact Page (`mrcs-wf-contact-v5.html`) — Figma node 162:2793

- No structural discrepancies. Section order, form fields, and layout all match Figma.
- Hero content width 850px (same delta, fix globally).

---

---

## 12. Location Detail (`mrcs-wf-location-v5.html`) — Figma node 190:94

### Hero
- Content width already at 850px. ✓

### Map Section
- Figma: 1240×514px, `box-shadow: 0px 4px 4px 0px rgba(0,0,0,0.25)`. Current: inside `.container` (max-width 1200px), height 360px, no shadow.
- → Remove `.container` wrapper on map, set height 514px, add box-shadow.

### Location Details Section
- "About This Location" label: current uses small uppercase text (`.location-details-label`). Figma shows 20px bold heading. → Change to h2-style 20px bold.
- Images: current 340×240px. Figma shows 429×233px. → Fix dimensions.

---

## 13. News Archive (`mrcs-wf-news-v5.html`) — Figma node 161:520

### Hero
- Content width: 850px (current 800px via global hero-inner). → Fix.

### News Section Layout
- Figma: filters and news grid are inside ONE gray `#f5f5f5` section ("Recent News") — filter row at top, 3-col 12-card grid below, then pagination. Current splits them: `news-filters` div is a separate element between hero and `news-grid-section`. → Move filters inside the gray section.
- Current has a `util-bar` above the header (not in Figma). → Remove.
- Grid width: Figma `w-[1072.5px]`, current inside `.container` (1200px). → Constrain grid to ~1072px max-width.

---

## 14. Event Post (`mrcs-wf-event-post-v5.html`) — Figma node 162:2558

### Hero / Body
- event-body-inner max-width: 800px → should be 850px.
- Image: Figma shows 850×450px placeholder. Current uses `ph-img` with 16:9 aspect-ratio at 800px width. → Set explicit 850px width, 450px height.

### Event Meta (Details/Organizer/Location)
- Figma: 3-col Details block spans full 1440px width with `gap-[74px]` between columns. Current uses `event-meta-inner` max-width 800px — too narrow, wrong. → Full section width, 3-col flex.

### Map
- Current: `event-map-inner` max-width 800px, height 250px. Figma: full width, 379px tall, box-shadow. → Remove max-width constraint, height 379px, add box-shadow.

### Callout
- Media: current 280×220px. Figma: 400×300px. → Fix.

---

## 15. Generic Page (`mrcs-wf-page-v5.html`) — Figma node 155:2326

### Hero
- Content width: 800px → 850px.

### Callout
- Media: current 280×220px. Figma shows 400×300px. → Fix.
- Callout inner max-width: currently 800px. Figma is `px-[100px]` on 1440 section. → Widen to use section padding pattern, not 800px inner.

---

## AUDIT COMPLETE — All 15 pages reviewed

### Global fix (applies to all pages):
- Hero / article content max-width: 800px → 850px

### Callout media fix (applies to: Service, Event Post, Generic Page):
- All callout media placeholders: 280×220 → 400×300px

